import ConnectionRecommendations from '@/components/connection-recommendations/ConnectionRecommendations';
import React from 'react';

const page = () => {
    return (
        <div>
            <ConnectionRecommendations/>
        </div>
    );
};

export default page;