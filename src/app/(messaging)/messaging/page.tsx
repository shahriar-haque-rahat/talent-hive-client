import Messaging from '@/components/messaging/Messaging';
import React from 'react';

const page = () => {
    return (
        <div>
            <Messaging/>
        </div>
    );
};

export default page;