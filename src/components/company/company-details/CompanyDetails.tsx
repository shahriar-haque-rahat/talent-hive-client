import React from 'react';

interface CompanyDetails {
    id: string
}

const CompanyDetails = async ({ id }: CompanyDetails) => {
    return (
        <div>

        </div>
    );
};

export default CompanyDetails;
